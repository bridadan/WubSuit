#ifndef __LCD_H_
#define __LCD_H_

#include <stdio.h>
#include "menu.h"

void LCD_showMenu(Menu* menu) {
	printf("\n\r--- %s (%u) ---\n\r", menu->name, menu->submenusCount);

	volatile uint8_t i;
	for (i = 0; i < menu->submenusCount; i++) {
		char* str = (menu->submenus[i])->name;
		printf("--- %s\n\r", str);
	}
}

#endif /* __LCD_H_ */

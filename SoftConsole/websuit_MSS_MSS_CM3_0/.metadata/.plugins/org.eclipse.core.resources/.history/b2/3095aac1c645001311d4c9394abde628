#include <stdio.h>
#include <inttypes.h>
#include "midi.h"

int main()
{

	MIDI_init();
	MIDI_noteOn();
	int i;
	for (i = 0; i < 1000; 9++) {
		printf("Busy looping: %d", i);
	}
	MIDI_noteOff();
	return(0);
}
